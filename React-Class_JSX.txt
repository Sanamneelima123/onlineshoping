React using html inside using in Javscript Also
JSX is Javscript syntax xml

import React from "react"

-- variables and functions --
const firstName="pavan";
const secondName="balla";

const user={
  firstName="pavan";
  secondName="balla";
}

function printName(){
   return ~My full name is ${user.firstName} ${user.secondName}~;
}


const App = () =>{
  return(
    <div>
      <h1>Hello World {firstName}</h1>
      <h1>Hello World {firstName(user)}</h1>
    </div>
  )
}